code should be strongly typed and well documented
use the most restricting modifier (private, public, protected) possible
use providers instead of ngmodule (provideHttpClient etc.)
use standalone and standalone imports (no ng modules)
make sure all pipes are imported (date, number, etc.)
use new structural directives 
use most current angular 19 and material dependencies  
I don't need tests
I want to use scss
